# Benchmark

- `ground-truth/` — phụ đề chuẩn do người gõ tay, **được commit**
- `output/` — kết quả OCR/ASR sinh ra, **không commit** (xem `.gitignore`)
- `clips.md` — nguồn từng clip và lý do chọn

Xem kế hoạch chi tiết ở `docs/sprint-5-benchmark.md`.
