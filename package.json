{
  "name": "translate-sub",
  "version": "0.1.0",
  "description": "Chrome extension: reads burned-in Vietnamese subtitles on web videos and shows an English translation, on-device.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "check": "node --test \"tests/**/*.test.mjs\""
  },
  "license": "MIT"
}
