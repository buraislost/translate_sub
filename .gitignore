# Thư viện tải về — dung lượng lớn, không commit
vendor/

# Model đã tải
*.traineddata
*.onnx
*.wasm

# Output benchmark
benchmark/output/

node_modules/
.DS_Store
*.log

# Khung hình lấy từ phim thật để đo OCR — chỉ giữ cục bộ, không commit
benchmark/fixtures/

# Đáp án gõ tay chứa lời thoại phim có bản quyền — chỉ giữ cục bộ
benchmark/ground-truth/*.json
