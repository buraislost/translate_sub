# Thư viện tải về — dung lượng lớn, không commit
vendor/

# Model đã tải
*.traineddata
*.onnx
*.wasm

# Output benchmark (giữ lại ground-truth)
benchmark/output/

node_modules/
.DS_Store
*.log

# Khung hình lấy từ phim thật để đo OCR — chỉ giữ cục bộ, không commit
benchmark/fixtures/
